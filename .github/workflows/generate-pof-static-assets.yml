name: Generate POF static explanation assets

on:
  push:
    branches: [main]
    paths:
      - 'scripts/generate_pof_static_assets.py'
      - 'scripts/generate_pof_static_assets_corrections.py'
      - 'scripts/apply_pof_asset_versions.py'
      - '.github/workflows/generate-pof-static-assets.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Pillow
        run: python -m pip install --disable-pip-version-check pillow

      - name: Generate and map static POF graphics
        run: |
          python scripts/generate_pof_static_assets.py
          python scripts/generate_pof_static_assets_corrections.py
          python scripts/apply_pof_asset_versions.py

      - name: Commit generated assets and mapping
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/explanation-images/principles-of-flight/static-v4
          git add 'app/practice/[subject]/components/principles-of-flight-visual.tsx'
          if git diff --cached --quiet; then
            echo "No asset or mapping changes"
            exit 0
          fi
          git commit -m "Generate QA-approved POF explanation assets"
          git push
