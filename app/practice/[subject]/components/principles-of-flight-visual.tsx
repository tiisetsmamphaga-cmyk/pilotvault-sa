"use client"

import type { Question } from "../types"

/**
 * Principles of Flight explanation visuals are temporarily disabled while the
 * image library is rebuilt and visually QA'd against the uploaded reference
 * handbook. Question reference images are rendered separately and remain
 * available.
 */
export function PrinciplesOfFlightVisual({ question: _question }: { question: Question }) {
  return null
}
